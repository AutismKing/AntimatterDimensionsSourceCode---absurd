/* import { RebuyableMechanicState, SetPurchasableMechanicState } from "./game-mechanics";

class BreakEternityUpgradeState extends BitPurchasableMechanicState {
  constructor(config) {
    super(config);
    this.registerEvents(config.checkEvent, () => this.tryUnlock());
  }

  get set() {
    return player.break-eternity-upgrades;

  }  */